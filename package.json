{
  "name": "agimmo-ma-astro-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler pages deploy dist --project-name=agimmo-ma",
    "deploy:pages": "npm run build && npm run deploy",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^6.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}

{
  "name": "agimmo-ma-astro-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "node -e \"console.log('⏭  No-op: Cloudflare Pages deploys automatically via Git integration after build. No API token needed.')\"",
    "check": "astro check",
    "postinstall": "node ./scripts/fix-wrangler-command.mjs"
  },
  "dependencies": {
    "astro": "^6.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
